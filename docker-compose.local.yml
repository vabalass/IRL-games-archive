volumes:
  games_project_local_postgres_data: {}
  games_project_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: games_project_local_django
    container_name: games_project_local_django
    depends_on:
      - postgres
      - mailpit
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: games_project_production_postgres
    container_name: games_project_local_postgres
    volumes:
      - games_project_local_postgres_data:/var/lib/postgresql/18/docker
      - games_project_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: games_project_local_mailpit
    ports:
      - "8025:8025"
